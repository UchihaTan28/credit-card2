import { Changes1Directive } from './changes1.directive';

describe('Changes1Directive', () => {
  it('should create an instance', () => {
    const directive = new Changes1Directive();
    expect(directive).toBeTruthy();
  });
});
