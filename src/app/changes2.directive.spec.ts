import { Changes2Directive } from './changes2.directive';

describe('Changes2Directive', () => {
  it('should create an instance', () => {
    const directive = new Changes2Directive();
    expect(directive).toBeTruthy();
  });
});
